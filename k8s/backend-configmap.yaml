# 비밀이 아닌 환경변수 — git에 올려도 됩니다.
# API 키 같은 민감 정보는 backend-secret.yaml 에 넣으세요.
apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: libraian
data:
  APP_ENV:        "production"
  LOG_LEVEL:      "INFO"
  CLOVA_MODEL:    "HCX-DASH-002"
  CLOVA_BASE_URL: "https://clovastudio.stream.ntruss.com/v1/openai"
  NARU_API_URL:   "http://data4library.kr/api"
  NARU_LIB_CODE:  ""
