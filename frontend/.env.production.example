# 배포 환경 — 방식에 따라 아래 중 하나를 선택해서 .env.production 으로 복사

# ① 같은 서버(nginx) 또는 Docker Compose — nginx가 /api를 백엔드로 프록시
VITE_API_URL=/api

# ② 분리 호스팅(Vercel + 별도 서버) — 백엔드 서버의 실제 주소를 입력
# VITE_API_URL=https://api.yourdomain.com/api
